services:
  - type: web
    name: elevenlabs-dialogue
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ELEVENLABS_API_KEY
        sync: false
